spring.application.name=keystone-backend

# Primary Datasource Credentials
spring.datasource.url=jdbc:postgresql://localhost:5433/postgres
spring.datasource.username=postgres
spring.datasource.password=Keystone@123

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

## Flyway Configuration (Uses spring.datasource automatically)
#spring.flyway.enabled=true
#spring.flyway.locations=classpath:db/migration

# JWT Configuration
jwt.secret=KeystoneFieldServiceManagementProjectSecretKey2026SprsingBootJWT123456789
jwt.expiration=86400000

server.port=8080